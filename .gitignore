# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
coverage
back_end/coverage

# Secrets — never commit real env values, only the .env.example templates
.env
back_end/.env
nutrition_service/.env

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.coverage
htmlcov/
.venv
venv/
ENV/

# Backend's JSON-file mock database (runtime data, not source)
back_end/data/

# MongoDB dumps written by the db-backup compose service — never commit
backups/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

